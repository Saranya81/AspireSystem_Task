import { CubeDemoPipe } from './cube-demo.pipe';

describe('CubeDemoPipe', () => {
  it('create an instance', () => {
    const pipe = new CubeDemoPipe();
    expect(pipe).toBeTruthy();
  });
});
